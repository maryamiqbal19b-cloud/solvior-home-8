import type { Metadata } from "next";
import Link from "next/link";
import { Eyebrow, Reveal } from "../components/Reveal";
import { CoreFeatures } from "../components/CoreFeatures";
import { Process } from "../components/Process";
import { SERVICES } from "../components/data";

export const metadata: Metadata = {
  title: "Features — Solvior",
  description: "Explore everything Solvior brings to the table — quick solutions, expert advice, strategic planning, and efficient operations.",
};

export default function FeaturesPage() {
  return (
    <>
      {/* Page header banner */}
      <section className="bg-[#0B1B33] text-white px-6 md:px-10 py-20 text-center">
        <Eyebrow dark>Explore Solvior</Eyebrow>
        <h1 className="font-display text-4xl md:text-5xl font-semibold mb-4 max-w-2xl mx-auto">
          Everything we bring to your business
        </h1>
        <p className="text-white/60 max-w-xl mx-auto">
          From quick fixes to long-term strategy, here&apos;s a closer look at how we help businesses grow with clarity and follow-through.
        </p>
      </section>

      {/* Core features (reused component) */}
      <CoreFeatures />

      {/* Services list */}
      <section className="py-24 px-6 md:px-10 bg-white">
        <div className="max-w-5xl mx-auto">
          <Reveal className="text-center mb-14">
            <Eyebrow>What&apos;s Included</Eyebrow>
            <h2 className="font-display text-3xl md:text-4xl font-semibold text-[#0B1B33] max-w-2xl mx-auto">
              A closer look at what we deliver
            </h2>
          </Reveal>

          <div className="divide-y divide-[#0B1B33]/10 border-t border-b border-[#0B1B33]/10">
            {SERVICES.map((s, i) => (
              <Reveal key={s.n} delay={i * 60}>
                <div className="group grid grid-cols-1 md:grid-cols-12 gap-4 md:items-center py-7 hover:bg-[#EEF2FA] transition-colors duration-300 px-2 md:px-4 rounded-xl">
                  <span className="md:col-span-1 font-display text-2xl text-[#2563EB] font-semibold">
                    {s.n}
                  </span>
                  <h3 className="md:col-span-4 font-display text-xl font-semibold text-[#0B1B33]">
                    {s.title}
                  </h3>
                  <p className="md:col-span-6 text-[#0B1B33]/60 leading-relaxed">{s.copy}</p>
                  <span className="md:col-span-1 flex md:justify-end">
                    <span className="w-9 h-9 rounded-full border border-[#0B1B33]/15 flex items-center justify-center group-hover:bg-[#2563EB] group-hover:border-[#2563EB] transition-colors">
                      <svg
                        className="w-4 h-4 text-[#0B1B33] group-hover:text-white transition-colors"
                        fill="none"
                        viewBox="0 0 24 24"
                        stroke="currentColor"
                        strokeWidth={2}
                      >
                        <path strokeLinecap="round" strokeLinejoin="round" d="M17 8l4 4m0 0l-4 4m4-4H3" />
                      </svg>
                    </span>
                  </span>
                </div>
              </Reveal>
            ))}
          </div>
        </div>
      </section>

      {/* Process (reused component) */}
      <Process />

      {/* CTA */}
      <section className="py-20 px-6 md:px-10 bg-gradient-to-r from-[#1E3A8A] to-[#2563EB] text-center">
        <h2 className="font-display text-3xl md:text-4xl font-semibold text-white mb-6">
          Ready to put this to work for you?
        </h2>
        <Link
          href="/contact"
          className="inline-block bg-white text-[#1E3A8A] font-semibold px-8 py-3.5 rounded-full hover:bg-[#F6F3EC] transition-colors"
        >
          Get in touch
        </Link>
      </section>
    </>
  );
}
